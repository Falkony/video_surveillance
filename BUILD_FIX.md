# 🚀 Исправление медленной сборки

## Что было сделано:

### 1. Оптимизация Nitro (SSR движок)
```typescript
nitro: {
  prerender: {
    routes: ['/'],
    crawlLinks: false,      // ← Отключен автоматический обход ссылок
    failOnError: false      // ← Не останавливаться на ошибках
  },
  experimental: {
    wasm: false             // ← Отключен WebAssembly
  }
}
```

### 2. Оптимизация Vite
- Добавлено разделение чанков (code splitting)
- Swiper вынесен в отдельный чанк
- Nuxt vendor библиотеки разделены

### 3. Отключены deprecation warnings
```json
"scripts": {
  "build": "NODE_OPTIONS='--no-deprecation' nuxt build",
  "dev": "NODE_OPTIONS='--no-deprecation' nuxt dev"
}
```

### 4. Оптимизация sitemap
```typescript
sitemap: {
  cacheMaxAgeSeconds: 3600  // ← Кеширование на 1 час
}
```

### 5. Экспериментальные функции
```typescript
experimental: {
  payloadExtraction: false  // ← Отключена экстракция payload (ускорение)
}
```

---

## 🔧 Что делать дальше:

### Вариант 1: Быстрый билд (рекомендуется для разработки)

```bash
# Остановите текущий процесс (Ctrl+C)
# Очистите кеш
rm -rf .nuxt node_modules/.cache .output

# Запустите сборку
pnpm build
```

### Вариант 2: Билд без предупреждений (Windows)

```powershell
# PowerShell
$env:NODE_OPTIONS="--no-deprecation"
pnpm build
```

```cmd
# CMD
set NODE_OPTIONS=--no-deprecation
pnpm build
```

### Вариант 3: Разработка без SSR (если нужна скорость)

Временно отключите SSR для быстрой разработки:

```typescript
// nuxt.config.ts
ssr: false  // для разработки
```

**Важно!** Перед деплоем верните `ssr: true`

---

## ⏱️ Ожидаемое время сборки:

- **До оптимизации**: 2-5 минут (может зависать)
- **После оптимизации**: 30-90 секунд

---

## 🐛 Если сборка всё ещё медленная:

### 1. Проверьте наличие .env файла
Если его нет, создайте пустой:
```bash
touch .env
```

### 2. Увеличьте memory для Node.js
```json
"scripts": {
  "build": "NODE_OPTIONS='--no-deprecation --max-old-space-size=4096' nuxt build"
}
```

### 3. Отключите type checking во время сборки
```typescript
// nuxt.config.ts
typescript: {
  typeCheck: false
}
```

### 4. Временно отключите модули SEO для тестирования
```typescript
modules: [
  '@nuxt/eslint',
  '@nuxt/image',
  '@nuxt/ui',
  '@nuxt/content',
  // '@nuxtjs/sitemap',  // ← закомментируйте
  // '@nuxtjs/robots'    // ← закомментируйте
]
```

После проверки верните их обратно.

---

## 📊 Мониторинг сборки

Чтобы увидеть, что именно тормозит:

```bash
# Запустите с анализом
NODE_OPTIONS='--no-deprecation' pnpm build --analyze

# Или с подробным логом
DEBUG=nuxt:* pnpm build
```

---

## 🎯 Что влияет на скорость:

1. **Better-sqlite3** - компилируется при первой установке
2. **@nuxt/content** - индексирует все .yml/.md файлы
3. **@nuxt/image** - оптимизирует изображения
4. **SSR** - рендерит страницы на сервере
5. **Sitemap** - генерирует карту сайта

---

## ✅ Чек-лист быстрого билда:

- [ ] Очищен кеш (`.nuxt`, `node_modules/.cache`, `.output`)
- [ ] Обновлены скрипты в `package.json`
- [ ] Создан файл `.env` (даже пустой)
- [ ] Выключены deprecation warnings
- [ ] Оптимизирован `nuxt.config.ts`

---

## 🚀 Финальные команды:

```bash
# Полная очистка и пересборка
rm -rf .nuxt node_modules/.cache .output
pnpm build

# Если нужна скорость в разработке
pnpm dev
```

---

## 💡 Совет

Для production сборки все оптимизации уже применены. 
Первый билд может быть медленным из-за компиляции native модулей (better-sqlite3).
Последующие билды будут быстрее благодаря кешированию.

---

**Дата**: 24 декабря 2025  
**Статус**: ✅ Оптимизировано

