# 🔤 Обновление шрифтов - переход на локальные

## Что изменилось

Проект теперь использует **локальные шрифты** через `@fontsource/inter` вместо загрузки из Google Fonts.

### Преимущества:

- ✅ Не зависит от доступа к Google Fonts
- ✅ Работает в любой стране без блокировок
- ✅ Быстрее загружается (шрифты в локальных файлах)
- ✅ Лучше для приватности пользователей
- ✅ Стабильная работа на серверах с ограниченным интернетом

## Инструкции по обновлению на сервере

### 1. Подключитесь к серверу

```bash
ssh root@ваш_ip_адрес
```

### 2. Перейдите в директорию проекта

```bash
cd /var/www/video-surveillance
```

### 3. Получите последние изменения

```bash
# Если через Git
git pull origin main

# Или если проект обновляется вручную - просто загрузите новые файлы
```

### 4. Установите новые зависимости

```bash
# Установка @fontsource/inter
pnpm install
```

### 5. Очистите старые сборки

```bash
rm -rf .nuxt .output node_modules/.cache
```

### 6. Пересоберите проект

```bash
pnpm build
```

### 7. Перезапустите приложение

```bash
pm2 restart video-surveillance

# Проверьте статус
pm2 status

# Проверьте логи (не должно быть ошибок про googleapis)
pm2 logs video-surveillance --lines 50
```

## Что было изменено в коде

### 1. `package.json`

Добавлена зависимость:

```json
"@fontsource/inter": "^5.1.0"
```

### 2. `app/assets/css/main.css`

Вместо Google Fonts теперь используется локальный импорт:

```css
@import "@fontsource/inter/400.css";
@import "@fontsource/inter/500.css";
@import "@fontsource/inter/600.css";
@import "@fontsource/inter/700.css";
```

### 3. `nuxt.config.ts`

Отключена загрузка шрифтов из @nuxt/ui:

```javascript
ui: {
  fonts: false
}
```

## Проверка

После обновления откройте сайт и проверьте:

1. ✅ В логах PM2 нет ошибок про `googleapis.com`
2. ✅ Шрифты корректно отображаются
3. ✅ Сайт загружается быстро

```bash
# Проверьте логи
pm2 logs video-surveillance --lines 100 | grep -i "font\|googleapis"
```

Если не находит ошибок - всё работает отлично! 🎉

## Альтернативный вариант (если нужны другие шрифты)

Если хотите использовать другие шрифты из `@fontsource`:

```bash
# Посмотрите доступные шрифты на https://fontsource.org/

# Установите нужный шрифт
pnpm add @fontsource/roboto
# или
pnpm add @fontsource/open-sans
```

Затем обновите `app/assets/css/main.css`:

```css
@import "@fontsource/roboto/400.css";
@import "@fontsource/roboto/700.css";

@theme {
  --font-sans: 'Roboto', system-ui, sans-serif;
}
```

## Откат (если что-то пошло не так)

Если нужно вернуться к старой версии:

```bash
cd /var/www/video-surveillance

# Откатите изменения
git checkout HEAD~1

# Установите зависимости
pnpm install

# Пересоберите
pnpm build

# Перезапустите
pm2 restart video-surveillance
```

---

**Вопросы?** Проверьте [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) раздел "Решение проблем".
