# 🔍 Диагностика проблемы с картинками

## Что проверить:

### 1. Откройте DevTools (F12)
- Перейдите на вкладку **Console**
- Есть ли ошибки 404?
- Какие именно пути не работают?

### 2. Проверьте Network
- Вкладка **Network** → фильтр **Img**
- Какой статус у картинок? (200, 404, pending?)
- Какие URL запрашиваются?

### 3. Проверьте, что сервер запущен
```bash
# Остановите сервер (Ctrl+C)
# Очистите кеш
rm -rf .nuxt node_modules/.cache

# Запустите заново
pnpm dev
```

## Возможные проблемы:

### Проблема 1: SSR блокирует статику
**Симптом**: 404 на все картинки

**Решение**: Временно отключите SSR для проверки
```typescript
// nuxt.config.ts
ssr: false  // только для теста!
```

Перезапустите `pnpm dev`. Если заработало - проблема в SSR.

### Проблема 2: Кеш не обновился
**Симптом**: Старые ошибки остались

**Решение**:
```bash
# Полная очистка
rm -rf .nuxt node_modules/.cache .output
pnpm dev
```

### Проблема 3: Vite не видит public
**Симптом**: 404 только в dev, в build работает

**Решение**: Уже добавлено в конфиг
```typescript
vite: {
  server: {
    fs: { strict: false }
  }
}
```

### Проблема 4: Неправильные пути
**Симптом**: Картинки ищутся не там

**Проверьте пути**:
```
✅ Правильно: /images/background.jpg
❌ Неправильно: images/background.jpg
❌ Неправильно: ./images/background.jpg
❌ Неправильно: ~/public/images/background.jpg
```

## Быстрое решение:

### Вариант 1: Перезапуск с очисткой
```powershell
# PowerShell
Remove-Item -Recurse -Force .nuxt, node_modules/.cache, .output -ErrorAction SilentlyContinue
pnpm dev
```

### Вариант 2: Временно отключить SSR
```typescript
// nuxt.config.ts - строка 43
ssr: false  // вместо true
```

Перезапустите. Если заработало - скажите, я настрою SSR правильно.

### Вариант 3: Проверить browser cache
- Откройте DevTools (F12)
- Правый клик на кнопке обновления
- "Очистить кеш и жесткая перезагрузка"

## Что мне нужно от вас:

Пожалуйста, скажите:

1. **Какие именно картинки не работают?**
   - [ ] Фон (background.jpg)
   - [ ] Логотип (logo.png)
   - [ ] Примеры в слайдере (examples/*.jpg)
   - [ ] Все картинки

2. **Что в консоли браузера?**
   - Есть ли ошибки 404?
   - Какие URL запрашиваются?

3. **Что в терминале?**
   - Есть ли ошибки при запуске dev сервера?

4. **После перезапуска с очисткой кеша помогло?**
   ```bash
   rm -rf .nuxt node_modules/.cache
   pnpm dev
   ```

---

## Временное решение (100% работает):

Если ничего не помогает, временно отключим SSR:

```typescript
// nuxt.config.ts
ssr: false
```

Это вернёт всё как было до SEO оптимизации. Картинки точно заработают.
Потом я настрою SSR правильно для production.

