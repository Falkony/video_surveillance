# 📱 Настройка отправки заявок в Telegram

## Что было сделано

✅ Создан серверный API endpoint `server/api/consultation.post.ts`
✅ Обновлен `nuxt.config.ts` с переменными окружения
✅ Обновлен `ConsultationModal.vue` для отправки данных на сервер
✅ Создан пример файла `.env.example`

## 🚀 Пошаговая инструкция

### Шаг 1: Получите токен бота

Если у вас уже есть токен - отлично! Если нет:

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Видеонаблюдение Заявки")
4. Введите username бота (например: "video_surveillance_bot")
5. Получите токен в формате: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### Шаг 2: Получите Chat ID

Есть несколько способов:

#### Способ 1: Для личных сообщений

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Запустите бота командой `/start`
3. Он пришлет ваш Chat ID (число вида `123456789`)

#### Способ 2: Для группы/канала

1. Добавьте вашего бота в группу/канал
2. Сделайте бота администратором (нужно для отправки сообщений)
3. Отправьте любое сообщение в группу
4. Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
5. Найдите в JSON `"chat":{"id":-1001234567890}` - это ваш Chat ID
   (для групп ID начинается с `-100`)

#### Способ 3: Используя curl

```bash
# Отправьте тестовое сообщение боту
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```

### Шаг 3: Создайте файл .env

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Configuration
NUXT_TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
NUXT_TELEGRAM_CHAT_ID=123456789

# Контактная информация (опционально)
NUXT_PUBLIC_PHONE=+79999999999
NUXT_PUBLIC_EMAIL=info@videosurveillance.ru
NUXT_PUBLIC_TELEGRAM=https://t.me/videosurveillance
NUXT_PUBLIC_SITE_NAME=Видеонаблюдение
NUXT_PUBLIC_SITE_URL=https://videosurveillance.ru
```

⚠️ **ВАЖНО:** Файл `.env` уже добавлен в `.gitignore` и не будет коммититься в Git!

### Шаг 4: Перезапустите сервер

```bash
# Остановите сервер (Ctrl+C)
# Запустите заново
pnpm dev
```

### Шаг 5: Протестируйте

1. Откройте сайт: http://localhost:3000/
2. Нажмите "Получить консультацию"
3. Заполните форму
4. Нажмите "Отправить"
5. Проверьте Telegram - должно прийти сообщение!

## 📋 Формат сообщения в Telegram

Заявка будет приходить в таком формате:

```
🆕 Новая заявка на консультацию

👤 Имя: Иван Иванов
📱 Телефон: +7 (999) 123-45-67
💬 Сообщение: Хочу установить камеры в офисе

⏰ Время: 04.12.2025, 15:30:45
```

## 🔧 Настройка формата сообщения

Если хотите изменить формат сообщения, отредактируйте файл `server/api/consultation.post.ts`:

```typescript
const message = `
🆕 *Новая заявка на консультацию*

👤 *Имя:* ${body.name}
📱 *Телефон:* ${body.phone}
${body.message ? `💬 *Сообщение:* ${body.message}` : ''}

⏰ *Время:* ${new Date().toLocaleString('ru-RU', { timeZone: 'Europe/Moscow' })}
`.trim()
```

### Можно добавить:

- Эмодзи и форматирование (Markdown)
- Дополнительные поля
- Кнопки (inline keyboard)
- Геолокацию
- И многое другое!

## 🌐 Деплой на production

### Vercel

1. Создайте проект на [Vercel](https://vercel.com)
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения:
   - `NUXT_TELEGRAM_BOT_TOKEN`
   - `NUXT_TELEGRAM_CHAT_ID`
4. Деплой!

### Netlify

1. Создайте проект на [Netlify](https://netlify.com)
2. В Settings → Environment variables добавьте:
   - `NUXT_TELEGRAM_BOT_TOKEN`
   - `NUXT_TELEGRAM_CHAT_ID`
3. Деплой!

### CloudFlare Pages

1. Создайте проект на [CloudFlare Pages](https://pages.cloudflare.com)
2. В Settings → Environment variables добавьте переменные
3. Деплой!

## 🐛 Возможные проблемы

### "Unauthorized" ошибка

- Проверьте правильность токена бота
- Убедитесь, что токен без пробелов и лишних символов

### "Chat not found" ошибка

- Проверьте Chat ID
- Убедитесь, что бот запущен (отправьте ему `/start`)
- Для групп: убедитесь, что бот добавлен и является администратором

### Сообщения не приходят

- Проверьте консоль браузера на ошибки
- Проверьте логи сервера
- Убедитесь, что файл `.env` создан и переменные загружены

### Проверка переменных окружения

Добавьте временно в `server/api/consultation.post.ts`:

```typescript
console.log('Bot Token:', config.telegramBotToken ? 'Loaded' : 'Missing')
console.log('Chat ID:', config.telegramChatId ? 'Loaded' : 'Missing')
```

## 📚 Дополнительные возможности

### 1. Отправка в несколько чатов

```typescript
const chatIds = [123456789, 987654321]
await Promise.all(
  chatIds.map(chatId => 
    $fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
      method: 'POST',
      body: { chat_id: chatId, text: message, parse_mode: 'Markdown' }
    })
  )
)
```

### 2. Добавление кнопок

```typescript
body: {
  chat_id: config.telegramChatId,
  text: message,
  parse_mode: 'Markdown',
  reply_markup: {
    inline_keyboard: [[
      { text: '✅ Обработано', callback_data: 'processed' },
      { text: '📞 Позвонить', url: `tel:${body.phone}` }
    ]]
  }
}
```

### 3. Сохранение в базу данных

Можно добавить сохранение заявок в базу данных (SQLite, PostgreSQL, MongoDB):

```typescript
// Пример с Prisma
await prisma.consultation.create({
  data: {
    name: body.name,
    phone: body.phone,
    message: body.message,
    createdAt: new Date()
  }
})
```

### 4. Email уведомления

Можно добавить отправку на email параллельно с Telegram:

```typescript
await sendEmail({
  to: 'info@videosurveillance.ru',
  subject: 'Новая заявка на консультацию',
  text: message
})
```

## 🔐 Безопасность

✅ Токен бота хранится на сервере (не доступен клиенту)
✅ Файл `.env` в `.gitignore`
✅ Валидация данных на сервере
✅ Обработка ошибок

## 📞 Поддержка

Если что-то не работает:
1. Проверьте все шаги выше
2. Проверьте логи в консоли
3. Проверьте, что сервер перезапущен после создания `.env`

---

**Готово!** Теперь все заявки с сайта будут автоматически приходить в ваш Telegram! 🎉

