# ⚡ Быстрое обновление на сервере

## Проблема, которую решаем

**Ошибка**: `https://fonts.googleapis.com/css2. Will retry in 1000ms. 3 retries left.`

Сервер не может подключиться к Google Fonts. Теперь используются локальные шрифты.

---

## 🚀 Обновление (копировать и выполнить на сервере)

```bash
# Подключитесь к серверу
ssh root@ваш_ip

# Перейдите в директорию проекта
cd /var/www/video-surveillance

# Получите обновления
git pull origin main

# Установите новые зависимости (включая @fontsource/inter)
pnpm install

# Очистите старые сборки
rm -rf .nuxt .output node_modules/.cache

# Пересоберите проект
pnpm build

# Перезапустите приложение
pm2 restart video-surveillance

# Проверьте статус и логи
pm2 status
pm2 logs video-surveillance --lines 30
```

---

## ✅ Проверка результата

После выполнения команд:

1. В логах `pm2 logs` **не должно** быть ошибок про `googleapis.com`
2. Приложение должно запуститься без предупреждений про шрифты
3. Сайт должен корректно отображаться

### Команда для проверки:

```bash
# Если команда ничего не находит - всё отлично!
pm2 logs video-surveillance --lines 100 | grep -i "googleapis"
```

---

## 🔄 Автоматический скрипт обновления

Если вы хотите упростить будущие обновления:

```bash
# Сделайте скрипт исполняемым
chmod +x update-server.sh

# Теперь для обновления достаточно запустить:
./update-server.sh
```

---

## 🆘 Если что-то пошло не так

### Проблема: PM2 не запускается

```bash
# Проверьте логи
pm2 logs video-surveillance --lines 50

# Попробуйте запустить вручную
node .output/server/index.mjs
```

### Проблема: Ошибка при установке зависимостей

```bash
# Удалите node_modules и переустановите
rm -rf node_modules pnpm-lock.yaml
pnpm install
pnpm build
pm2 restart video-surveillance
```

### Проблема: Всё ещё ошибка про Google Fonts

Проверьте, что в `nuxt.config.ts` есть:

```bash
grep -A 2 "ui:" nuxt.config.ts
```

Должно вывести:

```
ui: {
  fonts: false
},
```

Если нет - добавьте вручную:

```bash
nano nuxt.config.ts
# Добавьте после строки "ssr: false,"
```

---

## 📞 Нужна помощь?

Проверьте полную документацию:
- [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) - полное руководство по деплою
- [UPDATE_FONTS.md](./UPDATE_FONTS.md) - детали про обновление шрифтов
