# 📝 Модальное окно формы консультации

## Описание

Реализовано модальное окно с формой для получения консультации. Форма открывается при нажатии на кнопку "Получить консультацию" в любом месте сайта:
- В шапке сайта (Header)
- В главной Hero секции
- В нижней CTA секции

## Структура формы

### Поля формы

1. **Имя** (обязательное)
   - Placeholder: "Иван Иванов"
   - Валидация: не может быть пустым

2. **Телефон** (обязательное)
   - Placeholder: "+7 (999) 123-45-67"
   - Валидация: 
     - Не может быть пустым
     - Должен содержать только цифры, пробелы и символы +, -, (, )

3. **Сообщение** (необязательное)
   - Текстовая область (textarea)
   - Placeholder: "Расскажите о ваших потребностях в видеонаблюдении..."
   - 4 строки

### Кнопки

1. **Закрыть** - закрывает окно без отправки
2. **Отправить** - отправляет форму (с валидацией)

## Компоненты

### ConsultationModal.vue

Расположение: `app/components/ConsultationModal.vue`

```vue
<ConsultationModal v-model="isModalOpen" />
```

**Props:**
- `v-model` - булево значение для управления видимостью модального окна

**Особенности:**
- Автоматическая валидация полей
- Показ ошибок под полями
- Блокировка кнопок во время отправки
- Автоматический сброс формы после успешной отправки
- Toast уведомление об успешной отправке

## Интеграция

Модальное окно интегрировано в:

### 1. AppHeader.vue
```vue
const isModalOpen = ref(false)

<UButton 
  label="Консультация" 
  @click="openModal" 
/>
<ConsultationModal v-model="isModalOpen" />
```

### 2. index.vue (главная страница)
```vue
const isModalOpen = ref(false)

// В Hero секции
<UButton 
  label="Получить консультацию"
  @click="isModalOpen = true" 
/>

// В CTA секции
<UButton 
  label="Получить консультацию"
  @click="isModalOpen = true" 
/>

<ConsultationModal v-model="isModalOpen" />
```

## Валидация

Реализованная валидация:

```typescript
const validate = (state: any) => {
  const errors = []
  
  // Проверка имени
  if (!state.name) {
    errors.push({ 
      path: 'name', 
      message: 'Имя обязательно для заполнения' 
    })
  }
  
  // Проверка телефона
  if (!state.phone) {
    errors.push({ 
      path: 'phone', 
      message: 'Телефон обязателен для заполнения' 
    })
  }
  
  // Проверка формата телефона
  if (state.phone && !/^[\d\s\+\-\(\)]+$/.test(state.phone)) {
    errors.push({ 
      path: 'phone', 
      message: 'Некорректный формат телефона' 
    })
  }
  
  return errors
}
```

## Отправка формы

### Текущая реализация (заглушка)

```typescript
async function onSubmit() {
  loading.value = true
  
  // Имитация отправки (1 секунда задержка)
  await new Promise(resolve => setTimeout(resolve, 1000))
  
  // Показ уведомления
  toast.add({
    title: 'Заявка отправлена!',
    description: 'Мы свяжемся с вами в ближайшее время.',
    color: 'primary'
  })
  
  // Сброс формы и закрытие модального окна
  form.name = ''
  form.phone = ''
  form.message = ''
  loading.value = false
  isOpen.value = false
}
```

### 🔧 Как подключить реальную отправку

#### Вариант 1: Отправка на email через API

```typescript
async function onSubmit() {
  loading.value = true
  
  try {
    const response = await $fetch('/api/consultation', {
      method: 'POST',
      body: {
        name: form.name,
        phone: form.phone,
        message: form.message
      }
    })
    
    toast.add({
      title: 'Заявка отправлена!',
      description: 'Мы свяжемся с вами в ближайшее время.',
      color: 'primary'
    })
    
    // Сброс формы
    form.name = ''
    form.phone = ''
    form.message = ''
    isOpen.value = false
  } catch (error) {
    toast.add({
      title: 'Ошибка',
      description: 'Не удалось отправить заявку. Попробуйте позже.',
      color: 'error'
    })
  } finally {
    loading.value = false
  }
}
```

Создайте API endpoint: `server/api/consultation.post.ts`

```typescript
export default defineEventHandler(async (event) => {
  const body = await readBody(event)
  
  // Отправка email через сервис (например, Resend, SendGrid)
  // await sendEmail({
  //   to: 'info@videosurveillance.ru',
  //   subject: 'Новая заявка на консультацию',
  //   text: `Имя: ${body.name}\nТелефон: ${body.phone}\nСообщение: ${body.message}`
  // })
  
  return { success: true }
})
```

#### Вариант 2: Telegram Bot

```typescript
async function onSubmit() {
  loading.value = true
  
  try {
    const message = `
🆕 Новая заявка на консультацию

👤 Имя: ${form.name}
📱 Телефон: ${form.phone}
💬 Сообщение: ${form.message || 'Не указано'}
    `
    
    await $fetch(`https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage`, {
      method: 'POST',
      body: {
        chat_id: TELEGRAM_CHAT_ID,
        text: message
      }
    })
    
    toast.add({
      title: 'Заявка отправлена!',
      description: 'Мы свяжемся с вами в ближайшее время.',
      color: 'primary'
    })
    
    form.name = ''
    form.phone = ''
    form.message = ''
    isOpen.value = false
  } catch (error) {
    toast.add({
      title: 'Ошибка',
      description: 'Не удалось отправить заявку. Попробуйте позже.',
      color: 'error'
    })
  } finally {
    loading.value = false
  }
}
```

#### Вариант 3: Google Sheets

Используйте сервис вроде [SheetDB](https://sheetdb.io/) или Google Apps Script для сохранения заявок в таблицу.

## Дизайн

Модальное окно выполнено в едином стиле с сайтом:
- ✅ Использует компоненты Nuxt UI
- ✅ Адаптируется под темную/светлую тему
- ✅ Responsive дизайн (адаптивно на мобильных)
- ✅ Плавные анимации
- ✅ Accessibility (доступность)

## Настройка

### Изменить размер модального окна

```vue
<UModal v-model="isOpen" :ui="{ width: 'sm:max-w-lg' }">
  <!-- sm:max-w-md (по умолчанию)
       sm:max-w-lg (больше)
       sm:max-w-xl (еще больше) -->
</UModal>
```

### Изменить количество строк в текстовой области

```vue
<UTextarea
  v-model="form.message"
  :rows="6"  <!-- Измените с 4 на нужное значение -->
/>
```

### Добавить дополнительные поля

```vue
<UFormField
  label="Email"
  name="email"
>
  <UInput
    v-model="form.email"
    type="email"
    placeholder="example@mail.ru"
    size="lg"
  />
</UFormField>
```

## Тестирование

Проверьте работу формы:
- [ ] Модальное окно открывается при клике на кнопку
- [ ] Валидация работает (пустые обязательные поля)
- [ ] Валидация телефона работает
- [ ] Форма отправляется
- [ ] Показывается уведомление об успехе
- [ ] Форма сбрасывается после отправки
- [ ] Модальное окно закрывается
- [ ] Кнопка "Закрыть" работает
- [ ] Кнопка X в углу работает
- [ ] Клик вне модального окна закрывает его

## Будущие улучшения

- [ ] Добавить Google reCAPTCHA для защиты от спама
- [ ] Интеграция с CRM системой
- [ ] Email подтверждение для клиента
- [ ] Сохранение заявок в базу данных
- [ ] Админ-панель для просмотра заявок
- [ ] Маскирование телефона (автоформат +7 (999) 123-45-67)
- [ ] Календарь для выбора удобного времени звонка

---

**Статус:** ✅ Готово к использованию
**Следующий шаг:** Подключить реальную отправку на backend

