# 📋 Сводка изменений - Переход на локальные шрифты

## 🎯 Цель

Устранить ошибку: `https://fonts.googleapis.com/css2. Will retry in 1000ms. 3 retries left.`

## ✅ Что было сделано

### 1. Добавлена зависимость для локальных шрифтов

**Файл**: `package.json`

```json
"@fontsource/inter": "^5.1.0"
```

Шрифт Inter теперь загружается из node_modules, а не из интернета.

---

### 2. Обновлены стили для использования локальных шрифтов

**Файл**: `app/assets/css/main.css`

```css
@import "@fontsource/inter/400.css";
@import "@fontsource/inter/500.css";
@import "@fontsource/inter/600.css";
@import "@fontsource/inter/700.css";

@theme {
  --font-sans: 'Inter', system-ui, -apple-system, ...;
}
```

---

### 3. Отключена загрузка Google Fonts в Nuxt UI

**Файл**: `nuxt.config.ts`

```javascript
ui: {
  fonts: false
}
```

---

### 4. Создана документация

- ✅ `UPDATE_FONTS.md` - детальное руководство по обновлению шрифтов
- ✅ `QUICK_UPDATE.md` - быстрая инструкция для обновления на сервере
- ✅ `update-server.sh` - скрипт автоматического обновления
- ✅ `DEPLOYMENT_GUIDE.md` - обновлён с разделом про эту ошибку
- ✅ `README.md` - обновлён с информацией о шрифтах

---

## 🚀 Что делать дальше

### На локальной машине (уже сделано):

```bash
✅ pnpm install  # Установлены зависимости
```

### Закоммитить изменения:

```bash
git add .
git commit -m "fix: переход на локальные шрифты (@fontsource/inter) вместо Google Fonts

- Добавлена зависимость @fontsource/inter
- Обновлён main.css для импорта локальных шрифтов
- Отключена загрузка Google Fonts в nuxt.config.ts
- Добавлена документация по обновлению
- Создан скрипт автоматического обновления

Решает проблему: 'googleapis.com Will retry in 1000ms'"

git push origin main
```

### На сервере:

```bash
# 1. Подключитесь к серверу
ssh root@ваш_ip

# 2. Перейдите в проект
cd /var/www/video-surveillance

# 3. Обновите код
git pull origin main

# 4. Установите зависимости
pnpm install

# 5. Очистите кеш
rm -rf .nuxt .output node_modules/.cache

# 6. Пересоберите
pnpm build

# 7. Перезапустите
pm2 restart video-surveillance

# 8. Проверьте логи (не должно быть ошибок про googleapis)
pm2 logs video-surveillance --lines 30
```

---

## 🔍 Как проверить что всё работает

### 1. На сервере - проверка логов:

```bash
# Не должно находить упоминаний googleapis
pm2 logs video-surveillance --lines 100 | grep -i "googleapis"

# Если ничего не нашлось - отлично! ✅
```

### 2. В браузере:

1. Откройте ваш сайт
2. Откройте DevTools (F12) → Network
3. Обновите страницу
4. НЕ должно быть запросов к `fonts.googleapis.com`
5. Должны быть только локальные файлы шрифтов из `/node_modules/@fontsource/`

### 3. Проверка шрифтов:

1. Откройте DevTools → Elements → Computed
2. Выберите любой текстовый элемент
3. В разделе "font-family" должно быть: `Inter, system-ui, ...`

---

## 📊 Сравнение ДО и ПОСЛЕ

### ❌ ДО (проблема):

```
❌ Сервер пытается загрузить шрифты с fonts.googleapis.com
❌ Соединение не удаётся (блокировка / firewall)
❌ Ошибка: "Will retry in 1000ms. 3 retries left"
❌ Замедление загрузки сайта
❌ Возможны проблемы с отображением текста
```

### ✅ ПОСЛЕ (решение):

```
✅ Шрифты загружаются локально из node_modules
✅ Не требуется подключение к Google
✅ Работает в любой стране без блокировок
✅ Быстрая загрузка
✅ Стабильная работа
✅ Лучше для приватности
```

---

## 🎨 Почему выбран Inter?

- ✅ Современный, профессиональный шрифт
- ✅ Отличная читаемость на всех устройствах
- ✅ Похож на Public Sans (который был до этого)
- ✅ Бесплатный и open-source
- ✅ Широко используется (GitHub, Vercel, Tailwind UI)
- ✅ Доступен через @fontsource

---

## 🔄 Альтернативные варианты (если нужно)

Если захотите другой шрифт в будущем:

```bash
# Посмотрите доступные на https://fontsource.org/
pnpm add @fontsource/roboto
pnpm add @fontsource/open-sans
pnpm add @fontsource/poppins

# И обновите main.css
```

---

## 📞 Поддержка

Если возникнут вопросы:

1. Проверьте [QUICK_UPDATE.md](./QUICK_UPDATE.md)
2. Проверьте [UPDATE_FONTS.md](./UPDATE_FONTS.md)
3. Проверьте [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) → раздел "Решение проблем"

---

**Всё готово к деплою!** 🎉
