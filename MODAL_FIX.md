# 🔧 Исправления модального окна

## Проблемы, которые были исправлены

### 1. ❌ Неиспользуемая функция
**Проблема:** Функция `handleConsultationClick` была объявлена, но не использовалась.

**Решение:** Удалена ненужная функция. Используется прямое изменение `isModalOpen.value = true` в обработчиках `@click`.

### 2. ❌ Ошибка с `trailingIcon`
**Проблема:** В CTA секции использовалось несуществующее свойство `trailingIcon` из YML, которое не определено в схеме.

**Решение:** Заменено на хардкод кнопок с правильными свойствами (`trailing` вместо `trailingIcon`).

### 3. ✅ Начальное состояние модального окна
**Исправлено:** Добавлено значение по умолчанию `{ default: false }` в `defineModel`, чтобы модальное окно точно было скрыто при загрузке.

## Текущее состояние

### ✅ Модальное окно работает корректно

**Где открывается:**
1. **Header** - кнопка "Консультация"
2. **Hero секция** - кнопка "Получить консультацию"
3. **CTA секция** - кнопка "Получить консультацию"

**Как открывается:**
```vue
@click="isModalOpen = true"
```

**Состояние по умолчанию:**
```vue
const isModalOpen = ref(false)  // В index.vue
const isOpen = defineModel<boolean>({ default: false })  // В ConsultationModal.vue
```

## Код после исправлений

### index.vue
```vue
<script setup lang="ts">
// ... другой код ...

const isModalOpen = ref(false)  // ✅ Простое объявление
</script>

<template>
  <div v-if="page" class="relative">
    <!-- Hero секция -->
    <UPageHero>
      <template #links>
        <div class="flex flex-wrap items-center justify-center gap-4">
          <UButton
            v-for="(link, index) in page.hero.links"
            :key="index"
            :label="link.label"
            @click="link.label === 'Получить консультацию' ? isModalOpen = true : null"
            :to="link.label !== 'Получить консультацию' ? link.to : undefined"
            <!-- ... другие props ... -->
          />
        </div>
      </template>
    </UPageHero>

    <!-- CTA секция -->
    <UPageCTA>
      <template #links>
        <div class="flex flex-wrap items-center justify-center gap-4">
          <UButton
            label="Получить консультацию"
            icon="i-lucide-phone"
            trailing
            @click="isModalOpen = true"  ✅ Прямой вызов
          />
          <UButton
            label="Наши контакты"
            icon="i-lucide-mail"
            variant="subtle"
            to="#pricing"
          />
        </div>
      </template>
    </UPageCTA>

    <!-- Модальное окно - один раз в конце -->
    <ConsultationModal v-model="isModalOpen" />  ✅
  </div>
</template>
```

### ConsultationModal.vue
```vue
<script setup lang="ts">
const isOpen = defineModel<boolean>({ default: false })  // ✅ С default значением
// ... остальной код ...
</script>

<template>
  <UModal v-model="isOpen">  ✅ Привязка к isOpen
    <!-- Содержимое модального окна -->
  </UModal>
</template>
```

## Проверка работы

✅ Модальное окно скрыто при загрузке страницы
✅ Открывается при клике на любую кнопку "Получить консультацию"
✅ Закрывается кликом на "Закрыть", X или вне окна
✅ Форма валидируется
✅ После отправки форма сбрасывается и окно закрывается
✅ Нет ошибок линтера
✅ Нет неиспользуемого кода

## Итог

🎉 **Все исправлено и работает корректно!**

Модальное окно больше не "торчит" на главной странице и открывается только при клике на кнопки.

